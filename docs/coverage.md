# Coverage

Generated by `mcp-observatory crawl`. Every number below is computed from the crawl it describes, so this file and the scanned set can never disagree. Do not edit by hand.

Crawled at: 2026-09-21T23:02:03Z

## What the registry offered

The registry returned **34,630** entries. An entry is a registered name, not a server, and the two are not the same thing.

## What we scan, and what each rule costs

| Rule | Entries | Why |
| --- | ---: | --- |
| Dropped: no source to read | 8,004 | Hosted servers reached over HTTP carry no repository. There is nothing for a static analyser to read, and we never run a server to find out. |
| Collapsed: already counted | 5,270 | Separately registered names pointing at a repository another entry already named. Each repository is fetched and scanned once. |
| **Scanned: distinct repositories** | **21,356** | What the published index is actually about. |

8,004 + 5,270 + 21,356 = 34,630, against 34,630 entries seen. The arithmetic closes by construction: nothing disappears between two numbers.

## Why collapsing matters

The most-claimed repository carries **2,332** registered names. Counting names rather than repositories would let a single publisher set the headline figure. The names are not discarded: every one is recorded against its repository in the generated corpus, because a repository published under many names is itself worth reporting.

## Beyond the registry

These are **candidates, not a census.** The registry can be read to the end, so the figures above reproduce exactly. A code host serves at most 1,000 results per query at ten requests a minute, so what follows is what one bounded pass found, and a longer pass would find more. The search also cannot tell a server from something that merely uses one, so these are checked against their source before anything is published about them.

**13,291 of 13,694** repositories found this way appear nowhere in the registry (97%). The registry had already listed 403. The registered ecosystem is not the whole ecosystem, and this is the measurement that says by how much.

Found with 198 search requests, asking:

- `"@modelcontextprotocol/sdk" filename:package.json`
- `"from mcp.server" language:python`
