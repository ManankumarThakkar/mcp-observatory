# Coverage

Generated by `mcp-observatory crawl`. Every number below is computed from the crawl it describes, so this file and the scanned set can never disagree. Do not edit by hand.

Crawled at: 2026-09-21T22:00:38Z

## What the registry offered

The registry returned **34,215** entries. An entry is a registered name, not a server, and the two are not the same thing.

## What we scan, and what each rule costs

| Rule | Entries | Why |
| --- | ---: | --- |
| Dropped: no source to read | 8,001 | Hosted servers reached over HTTP carry no repository. There is nothing for a static analyser to read, and we never run a server to find out. |
| Collapsed: already counted | 5,209 | Separately registered names pointing at a repository another entry already named. Each repository is fetched and scanned once. |
| **Scanned: distinct repositories** | **21,005** | What the published index is actually about. |

8,001 + 5,209 + 21,005 = 34,215, against 34,215 entries seen. The arithmetic closes by construction: nothing disappears between two numbers.

## Why collapsing matters

The most-claimed repository carries **2,332** registered names. Counting names rather than repositories would let a single publisher set the headline figure. The names are not discarded: every one is recorded against its repository in the generated corpus, because a repository published under many names is itself worth reporting.
